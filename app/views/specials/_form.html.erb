<%= form_with(model: special, local: true) do |form| %>
  <% if special.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(special.errors.count, "error") %> prohibited this special from being saved:</h2>

      <ul>
      <% special.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :special_name %>
  </div>

  <div class="field">
    <%= form.label :text %>
    <%= form.text_field :text, id: :special_text %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
